@using Grievancemis.Manager
@*@model Grievancemis.Models.AssignTo*@
@model Grievancemis.Models.FilterModel
@{
    ViewBag.Title = "Case Assign";
    ViewBag.SubTitle = "Case Assign";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<link href="~/Content/fSelect.css" rel="stylesheet" />
<div class="page-header">
    <div class="page-block">
        <div class="page-header-title">
            <h2 class="mb-0">@ViewBag.Title</h2>
            <ul class="breadcrumb">
                @*<li class="breadcrumb-item"><a href="/">Home</a></li>*@
                <li class="breadcrumb-item" aria-current="page">@ViewBag.SubTitle</li>
            </ul>
        </div>
    </div>
</div>

<div class="grievances-list">
    <div class="box-design">
        @using (Html.BeginForm("GetCaseAssignGList", "Complain", FormMethod.Post, new { @id = "formid", @class = "", role = "form", autocomplete = "off", enctype = "multipart/form-data", noValidate = "novalidate" }))
        {
            <div class="flex-row">
                <div class="form-group">
                    @Html.LabelFor(model => model.StateId, htmlAttributes: new { @class = "control-label" })
                    <span style="color:red;">*</span>
                    @Html.DropDownListFor(model => model.StateId, CommonModel.GetState(), null, new { @class = "form-control", @required = "required" })
                    @Html.ValidationMessageFor(model => model.StateId, "", new { @class = "text-danger" })
                </div>

                <div class="form-group">
                    @Html.LabelFor(model => model.TypeGId, htmlAttributes: new { @class = "control-label" })
                    <span style="color:red;">*</span>
                    @Html.DropDownListFor(model => model.TypeGId, CommonModel.GetGrievanceType(), null, new { @class = "form-control", @required = "required" })
                    @Html.ValidationMessageFor(model => model.TypeGId, "", new { @class = "text-danger" })
                </div>

                <div class="form-btn">
                    <button id="search-button" class="btn btn-primary">Search</button>
                </div>
            </div>
        }
    </div>
</div>

<div id="subdata"></div>

@*------------Work Assign Model popup---------------------*@

<div class="modal fade animate" id="workAssignModal" tabindex="-1" role="dialog" aria-labelledby="workAssignModalLabel" aria-hidden="true"
     data-backdrop="static" data-keyboard="false">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="workAssignModalLabel">Case Assign To Panel Member </h5>
                <button type="button" class="close popup-close" data-dismiss="modal" aria-label="Close">
                    <i class="ti ti-x"></i>
                </button>
            </div>
            <div class="modal-body">
                <div class="row">
                    @Html.HiddenFor(x => x.GrievanceId_fk)
                    <div class="modal-design">
                        <p>
                            Case Id:
                            <span><strong id="assignCaseId"></strong></span>
                        </p>
                        <p>
                            Name:
                            <span><strong id="assignName"></strong></span>
                        </p>
                        <p>
                            Complain Date:
                            <span><strong id="assignCreatedOn"></strong></span>
                        </p>
                    </div>

                    @*<select class="multiselect" multiple="multiple">
                        <option value="January">January</option>
                        <option value="February">February</option>
                        <option value="March">March</option>
                        <option value="April">April</option>
                        <option value="January">January</option>
                        <option value="February">February</option>
                        <option value="March">March</option>
                        <option value="April">April</option>
                    </select>*@
                    @*<div class="form-group mar-bom">
                            @Html.LabelFor(model => model.User_Idfk, htmlAttributes: new { @class = "control-label" })
                            <span style="color:red;">*</span>
                            @Html.DropDownList("User_Idfk", CommonModel.GetpanelList(false), new { @class = "form-control", @multiple = "multiple", @required = "required" })
                            @Html.ValidationMessageFor(model => model.User_Idfk, "", new { @class = "text-danger" })
                        </div>*@

                    <div class="form-group mar-bom">
                        @Html.LabelFor(model => model.User_Idfk, htmlAttributes: new { @class = "control-label" })
                        <span style="color:red;">*</span>
                        @Html.DropDownList("User_Idfk", CommonModel.GetpanelList(false), new { @class = "form-control choices-multiple", @multiple = "multiple", @required = "required" })
                        @Html.ValidationMessageFor(model => model.User_Idfk, "", new { @class = "text-danger" })
                    </div>

                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary popup-close" data-dismiss="modal">Close</button>
                <button type="submit" id="confirmWorkAssign" class="btn btn-primary">Submit</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval");

    <script src="~/Content/fSelect.js"></script>
    <script>
        $(function () {
            $('.multiselect').fSelect();
        });
    </script>

    @*<script src="~/Scripts/JS/Master.js"></script>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
        <script type="text/javascript" src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>*@

    @*<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-multiselect/0.9.13/js/bootstrap-multiselect.js"></script>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-multiselect/0.9.13/css/bootstrap-multiselect.css">*@

    <!-- Include Bootstrap CSS and JS -->
    @*<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>*@

    <!-- Include Bootstrap Multiselect CSS and JS -->
    @*<link href="https://cdn.jsdelivr.net/npm/bootstrap-multiselect@1.1.0/dist/css/bootstrap-multiselect.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap-multiselect@1.1.0/dist/js/bootstrap-multiselect.min.js"></script>*@

    @*<link href="https://cdn.jsdelivr.net/npm/choices.js/public/assets/styles/choices.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/choices.js/public/assets/scripts/choices.min.js"></script>*@

        @*<link href="https://cdn.jsdelivr.net/npm/choices.js/public/assets/styles/choices.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/choices.js/public/assets/scripts/choices.min.js"></script>*@

    <script>
        $(document).ready(function () {
            $('#workAssignModal').modal({
                backdrop: 'static',
                keyboard: false
            });
            bindload();
            //$('#User_Idfk').fSelect();
            $('#search-button').on('click', function (e) {
                e.preventDefault();
                bindload();
            });
            document.addEventListener("click", e => {
                // Find the currently open modal
                const activeModal = document.querySelector(".modal.show");
                if (!activeModal) return;

                const modalContent = activeModal.querySelector(".modal-content");
                // If modalContent exists and the click is outside it, close the modal
                if (modalContent && !modalContent.contains(e.target)) {
                    $(activeModal).modal('hide'); // Bootstrap's method to close the modal
                }
            });
            $(document).on('click', '.popup-close', function () {
                $('#workAssignModal').modal();
                $('#workAssignModal').modal('hide');
            });
            //$('.popup-close').on('click', function () {
            //    $('#workAssignModal').modal('hide');
            //});

            $('#confirmWorkAssign').on('click', function () {
                var grievanceId = $('#GrievanceId_fk').val(); // Get the grievance ID
                var User_Idfks = $('#User_Idfk').val(); // Get selected User_Idfk

                // Validation
                if (!User_Idfks || User_Idfks.length === 0) {
                    toastr.error('Please select at least one role');
                    return false;
                }

                // Prepare data for the AJAX request
                var requests = User_Idfks.map(function (uId) {
                    return {
                        Grievance_Idfk: grievanceId,
                        User_Idfk: uId,
                    };
                });

                $.ajax({
                    type: "POST",
                    url: document.baseURI + "/Complain/AssignWork",
                    data: JSON.stringify(requests), // Send as JSON
                    contentType: "application/json; charset=utf-8", // Set content type
                    success: function (response) {
                        if (response.success) {
                            toastr.success(response.message);
                            $('#workAssignModal').modal('hide');
                        } else {
                            toastr.error(response.message);
                        }
                    },
                    error: function (xhr, status, error) {
                        toastr.error("An error occurred: " + error);
                    }
                });
            });
            //let choicesInstance;
            //choicesInstance = new Choices('#User_Idfk', { removeItemButton: true });
            //$('#workAssignModal').on('hidden.bs.modal', function () {
            //    if (choicesInstance) {
            //        choicesInstance.destroy(); // Clean up on modal close
            //        choicesInstance = null;
            //    }
            //});

        });
        //function defaults() {
        //    return labels.map((lbl, i) => ({ value: i, label: lbl }));
        //}

        function reset() {
            choicesInstance.clearChoices();
            //choicesInstance.setChoices(defaults(), 'value', 'label', false);
        }
        // Handle progress button click
        //document.querySelector('.popup-close').addEventListener('click', () => {
        //    debugger;
        //    const modal = document.querySelector('#workAssignModal');
        //    modal.style.display = 'none';

        //    if (choicesInstance) {
        //        choicesInstance.destroy();
        //        choicesInstance = null; // Allow reinitialization
        //    }
        //});

        function openWorkAssignPopup(id, caseId, name, createdOn, UserAspIds) {

            if (id != null && id != '') {
                $('#workAssignModal').modal();
                $('#workAssignModal').modal('show');
                $('#assignCaseId').html('');
                $('#assignName').html('');
                $('#assignCreatedOn').html('');

                //const choices=  new Choices('#User_Idfk');
                //document.getElementById("User_Idfk").options.length = 0;
                //$('#User_Idfk').empty();
                //const choices = new Choices('#User_Idfk');
                //choices.clearStore();

                $('#GrievanceId_fk').val(id);
                $('#assignCaseId').html(caseId);
                $('#assignName').html(name);
                $('#assignCreatedOn').html(createdOn);
                //const selectElement = document.querySelector('#User_Idfk');
                //selectElement.destroy();
                //let choices1;
                //if (!choices1) {
                //    const selectElement = document.querySelector('#User_Idfk');
                //    choices1 = new Choices(selectElement, { removeItemButton: true });
                //}
                //// Later, you can safely call:
                //choices1.clearChoices();
               
               
                //$('#User_Idfk').val("");
                //const selectElement = document.querySelector('#User_Idfk');
                //new Choices(selectElement, {
                //    removeItemButton: true,
                //});
                //$("#User_Idfk").Choices('refresh');
                //$('#User_Idfk').multiselect({
                //    includeSelectAllOption: true
                //});
                debugger;
               // $('#User_Idfk').fSelect('destroy');
                if ($('#User_Idfk').data('fSelect')) {
                    $('#User_Idfk').fSelect('destroy'); // Destroy the existing instance
                }
                $('#User_Idfk').fSelect(); // Reinitialize
               
               // const choicesfrt = new Choices($('#User_Idfk')[0]);
                if (UserAspIds) {
                  //var choices = new Choices('#User_Idfk', {
                  //       removeItemButton: true,

                  //      searchEnabled: true,          // Enable/disable search
                  //      itemSelectText: '',           // Custom text for selection
                  //    duplicateItemsAllowed: false, // Prevent duplicate selections
                  //    //choices: defaults()

                  //});
                    let str = UserAspIds;//'"1ea6f120-698b-4172-81c7-5126d8229dc9","fa207900-0224-4f12-aa80-cec4316bb871"';
                    let formattedArray = str.replace(/"/g, '').split(',').map(val => `${val}`);
                   // $('#User_Idfk').setSelectedValues(formattedArray);//["1ea6f120-698b-4172-81c7-5126d8229dc9","fa207900-0224-4f12-aa80-cec4316bb871"]
                    // $('#User_Idfk').fSelect('reload');

                    setSelectedValues('User_Idfk', formattedArray);
                    console.log(formattedArray);
                    //$('#User_Idfk').fSelect('reload');
                }
                else {
                    $('#User_Idfk').fSelect('reload');
                    //User_Idfkfselect.destroy();
                    //$('#User_Idfk').fSelect();
                   // $('#User_Idfk').fSelect('reload');
                   //var choices = new Choices('#User_Idfk', {
                   //     removeItemButton: true,
                   //     searchEnabled: true,          // Enable/disable search
                   //     itemSelectText: '',           // Custom text for selection
                   //    duplicateItemsAllowed: false, // Prevent duplicate selections
                   //    //choices: defaults()
                   //});
                    ////choices.passedElement.element.addEventListener('addItem', () => reset(), false);
                    ////choices.passedElement.element.addEventListener('removeItem', () => reset(), false);

                    ////function reset() {
                    ////    choices.clearChoices();
                    ////    choices.setChoices("", "value", "label", false);
                    ////}
                    ////choices.destroy();
                    ////choices.init();
                    //// choices.setChoiceByValue(['']);
                    ////choices.setChoiceByValue([]);
                }
                //$('#User_Idfk').remove();
                //$('#User_Idfk').trigger("change");
            }
            else {
                window.location.href = document.baseURI + "/Grievnce/GrievanceCaseAdd";
            }
        }
        // Function to set selected values
        function setSelectedValues(elementId, valuesArray) {
            // Update the underlying <select> element's value
            $('#' + elementId).val(valuesArray);

            // Refresh the fSelect instance to reflect the new values
            $('#' + elementId).fSelect('reload');
        }

        function bindload() {
            //debugger;
            $("#subdata").html(''); $('#div-download').empty();
            var filtermodel = new Object();
            filtermodel.StateId = $('#StateId').val() == '' ? '' : $('#StateId').val();
            filtermodel.TypeGId = $('#TypeGId').val() == '' ? '' : $('#TypeGId').val();
            // filtermodel.ToDt = $('#ToDt').val() == '' ? '' : $('#ToDt').val();
            $.ajax({
                type: "Get",
                url: document.baseURI + "/Complain/GetCaseAssignGList",
                data: filtermodel,
                //cache: false,
                success: function (res) {
                    if (res.IsSuccess) {
                        $("#subdata").html(res.Data);

                        //if ($.fn.dataTable.isDataTable('#tbl')) {
                        //    $("#tbl").dataTable().fnDestroy();
                        //}
                        //table = $('#tbl').DataTable({
                        //    //scrollY: "400px",
                        //    //scrollX: true,
                        //    //scrollCollapse: true,
                        //    paging: false,
                        //    "dom": '<"pull-left"f><"pull-right"l>tip',
                        //    //"ordering": false,
                        //    // order: [[1,0, 'desc']],
                        //    // pageLength: 100,
                        //    fixedColumns: {
                        //        leftColumns: 1,
                        //        rightColumns: 1
                        //    },
                        //    buttons: [{
                        //        extend: 'excel', text: '<span><i class="fa fa-download"></i> Download</span>', title: 'Participant Lists',//$('#IDDistrict option:selected').text() +
                        //        className: 'btn btn-primary button-icon mr-3 mt-1 mb-1',
                        //        filename: 'Participant Lists',
                        //        exportOptions: { modifier: { page: 'all' } }
                        //    }],
                        //});
                        //$('.dataTables_filter input[type="search"]').css(
                        //    { 'width': '200px', 'display': 'inline-block' }
                        //);
                        //$('#div-download').empty();
                        //table.buttons().container().appendTo($('#div-download'));
                    }
                    else {
                        $("#subdata").html('<h5 class="text-danger">' + res.Data + '</h3>');
                    }
                },
                error: function (req, error) {
                    if (error === 'error') { error = req.statusText; }
                    var errormsg = 'There was a communication error: ' + error;
                    //Do To Message display
                    $("#subdata").html('<h5 class="text-danger">' + errormsg + '</h3>');
                }
            });
        }


    </script>


}
